// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/fogo-sh/grackdb/ent/repository"
	"github.com/fogo-sh/grackdb/ent/repositorytechnology"
	"github.com/fogo-sh/grackdb/ent/technology"
)

// RepositoryTechnologyCreate is the builder for creating a RepositoryTechnology entity.
type RepositoryTechnologyCreate struct {
	config
	mutation *RepositoryTechnologyMutation
	hooks    []Hook
}

// SetType sets the "type" field.
func (rtc *RepositoryTechnologyCreate) SetType(r repositorytechnology.Type) *RepositoryTechnologyCreate {
	rtc.mutation.SetType(r)
	return rtc
}

// SetRepositoryID sets the "repository" edge to the Repository entity by ID.
func (rtc *RepositoryTechnologyCreate) SetRepositoryID(id int) *RepositoryTechnologyCreate {
	rtc.mutation.SetRepositoryID(id)
	return rtc
}

// SetRepository sets the "repository" edge to the Repository entity.
func (rtc *RepositoryTechnologyCreate) SetRepository(r *Repository) *RepositoryTechnologyCreate {
	return rtc.SetRepositoryID(r.ID)
}

// SetTechnologyID sets the "technology" edge to the Technology entity by ID.
func (rtc *RepositoryTechnologyCreate) SetTechnologyID(id int) *RepositoryTechnologyCreate {
	rtc.mutation.SetTechnologyID(id)
	return rtc
}

// SetTechnology sets the "technology" edge to the Technology entity.
func (rtc *RepositoryTechnologyCreate) SetTechnology(t *Technology) *RepositoryTechnologyCreate {
	return rtc.SetTechnologyID(t.ID)
}

// Mutation returns the RepositoryTechnologyMutation object of the builder.
func (rtc *RepositoryTechnologyCreate) Mutation() *RepositoryTechnologyMutation {
	return rtc.mutation
}

// Save creates the RepositoryTechnology in the database.
func (rtc *RepositoryTechnologyCreate) Save(ctx context.Context) (*RepositoryTechnology, error) {
	var (
		err  error
		node *RepositoryTechnology
	)
	if len(rtc.hooks) == 0 {
		if err = rtc.check(); err != nil {
			return nil, err
		}
		node, err = rtc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*RepositoryTechnologyMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = rtc.check(); err != nil {
				return nil, err
			}
			rtc.mutation = mutation
			if node, err = rtc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(rtc.hooks) - 1; i >= 0; i-- {
			if rtc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = rtc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, rtc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (rtc *RepositoryTechnologyCreate) SaveX(ctx context.Context) *RepositoryTechnology {
	v, err := rtc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (rtc *RepositoryTechnologyCreate) Exec(ctx context.Context) error {
	_, err := rtc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (rtc *RepositoryTechnologyCreate) ExecX(ctx context.Context) {
	if err := rtc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (rtc *RepositoryTechnologyCreate) check() error {
	if _, ok := rtc.mutation.GetType(); !ok {
		return &ValidationError{Name: "type", err: errors.New(`ent: missing required field "type"`)}
	}
	if v, ok := rtc.mutation.GetType(); ok {
		if err := repositorytechnology.TypeValidator(v); err != nil {
			return &ValidationError{Name: "type", err: fmt.Errorf(`ent: validator failed for field "type": %w`, err)}
		}
	}
	if _, ok := rtc.mutation.RepositoryID(); !ok {
		return &ValidationError{Name: "repository", err: errors.New("ent: missing required edge \"repository\"")}
	}
	if _, ok := rtc.mutation.TechnologyID(); !ok {
		return &ValidationError{Name: "technology", err: errors.New("ent: missing required edge \"technology\"")}
	}
	return nil
}

func (rtc *RepositoryTechnologyCreate) sqlSave(ctx context.Context) (*RepositoryTechnology, error) {
	_node, _spec := rtc.createSpec()
	if err := sqlgraph.CreateNode(ctx, rtc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (rtc *RepositoryTechnologyCreate) createSpec() (*RepositoryTechnology, *sqlgraph.CreateSpec) {
	var (
		_node = &RepositoryTechnology{config: rtc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: repositorytechnology.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: repositorytechnology.FieldID,
			},
		}
	)
	if value, ok := rtc.mutation.GetType(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeEnum,
			Value:  value,
			Column: repositorytechnology.FieldType,
		})
		_node.Type = value
	}
	if nodes := rtc.mutation.RepositoryIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   repositorytechnology.RepositoryTable,
			Columns: []string{repositorytechnology.RepositoryColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: repository.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.repository_technologies = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := rtc.mutation.TechnologyIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   repositorytechnology.TechnologyTable,
			Columns: []string{repositorytechnology.TechnologyColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: technology.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.technology_repositories = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// RepositoryTechnologyCreateBulk is the builder for creating many RepositoryTechnology entities in bulk.
type RepositoryTechnologyCreateBulk struct {
	config
	builders []*RepositoryTechnologyCreate
}

// Save creates the RepositoryTechnology entities in the database.
func (rtcb *RepositoryTechnologyCreateBulk) Save(ctx context.Context) ([]*RepositoryTechnology, error) {
	specs := make([]*sqlgraph.CreateSpec, len(rtcb.builders))
	nodes := make([]*RepositoryTechnology, len(rtcb.builders))
	mutators := make([]Mutator, len(rtcb.builders))
	for i := range rtcb.builders {
		func(i int, root context.Context) {
			builder := rtcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*RepositoryTechnologyMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, rtcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, rtcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, rtcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (rtcb *RepositoryTechnologyCreateBulk) SaveX(ctx context.Context) []*RepositoryTechnology {
	v, err := rtcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (rtcb *RepositoryTechnologyCreateBulk) Exec(ctx context.Context) error {
	_, err := rtcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (rtcb *RepositoryTechnologyCreateBulk) ExecX(ctx context.Context) {
	if err := rtcb.Exec(ctx); err != nil {
		panic(err)
	}
}
